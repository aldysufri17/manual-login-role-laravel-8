<div>
    <div class="form-group">
        <label for="{{$name}}">{{$title}}</label>
        <input type="{{$type}}" value="{{$value}}" name="{{$name}}" class="form-control" id="{{$name}}" {{$autofocus}}>
    </div>
</div>